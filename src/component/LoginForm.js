import React, { Component } from 'react';
import { View, Text } from 'react-native';
import { Button, Card, CardSection } from './common';

class LoginForm extends Compnent {
  render() {
    return (
      <Card>
        <CardSection />
        <CardSection />
        <CardSection />
      </Card>
    );
  }
}

export default LoginForm;
